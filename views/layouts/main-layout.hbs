<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ pageTitle }}</title>
    {{#if productCSS }}
        <link rel="stylesheet" href="/css/shop.css">
    {{/if }}
    {{#if addProductCSS }}
        <link rel="stylesheet" href="/css/add-product.css">
    {{/if }}
</head>
<body>
    <header>
        <nav>
            <ul>
                <li><a href="/" class="{{#if activeShop }}active{{/if }}">My Product</a></li>
                <li><a href="/admin/add-product" class="{{#if activeAddProduct }}active{{/if }}">Add Products</a></li>
            </ul>
        </nav>
    </header>

    {{{ body }}}
</body>
</html>